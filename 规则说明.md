# 资源选择规则说明

## 基础规则

设备或起点选择下游节点的规则如下：

1. **产品匹配：** 必须从设置的允许加工产品包括了当前节点所设置的产品的下游节点中选择。如果没有设置产品则表示能加工所有产品。

2. **优先空闲：** 在满足第一条件的情况下，优先选择空闲的设备。

3. **距离最近：** 如果有多台满足第二条件的设备，则选择距离最近的。

4. **随机选择：** 如果有多台第三条件相同的设备，则随机选择一台。

---

## 运输规则

产品在节点之间通过连线进行运输，运输模式分为连续运输和离散运输两种。

### 1. 运输时间计算

运输时间由连线长度和运输速度决定，计算公式为：

**运输时间（秒） = 连线长度（米） / 运输速度（米/秒）**

- 连线长度根据画布上设备位置自动计算
- 运输速度默认为 1 m/s，可在连线属性中修改
- **特殊规则：** 起点到下游节点、节点到终点的连线运输时间为 0 秒

### 2. 连续运输模式

连续运输模拟传送带式的运输方式：

1. **运输方式：** 产品到达后立即开始运输，无需等待
2. **并发控制：**
   - 默认为"无限制并发"，即不限制同时运输的产品数量
   - 关闭"无限制并发"后，可设置最大并发数，当路线上正在运输的产品数达到最大并发数时，新产品需等待
3. **运输容量：** 每次运输1个产品
4. **适用场景：** 流水线生产、传送带

### 3. 离散运输模式

离散运输模拟小车批量运输方式：

1. **运输方式：** 产品需要等待空闲小车才能开始运输
2. **小车数量：** 可设置1~50辆小车，每辆小车独立运行
3. **单次运量：** 每辆小车单次可运输1~100个产品
4. **运输批次：** 每次运输生成一个批次号，便于追踪
5. **适用场景：** AGV小车、叉车运输

### 4. 运输启动条件

产品加工完成变为"等待运输"状态后，系统按以下条件判断是否可以启动运输：

**连续运输模式：**
- 无限制并发：始终可以启动运输
- 有限制并发：当前路线上正在运输的产品数 < 最大并发数时可以启动

**离散运输模式：**
- 当前路线上有空闲小车（idle_carts > 0）时可以启动

### 5. 运输完成后的处理

产品运输到达目标设备后：

1. **状态更新：** 产品状态从"运输中"变为目标设备对应的状态
2. **路径利用率更新：** 连线的运输记录增加
3. **触发后续操作：** 根据目标设备类型触发相应处理逻辑

---

## 动态平衡算法(在制品)

当某个产品从一个节点（可以是起点、设备、缓冲区、仓库、临时堆场）出发时，按如下步骤执行：

1. **检测下游节点数量：** 检测当前节点的允许加工本产品的直接下游节点数量。如果只有1个则不做任何额外处理，直接选择该节点；如果大于1个，则进入下一步。

2. **获取剩余路线：** 从当前产品的产品工艺路线中，获取分别从当前节点允许加工本产品的不同下游节点为起点的剩余路线。

3. **计算在制品总量：** 分别计算每条剩余路线中的所有节点和连线的当前在制品总量。计算公式为：在制品总量 = Σ(节点wip) + Σ(连线inflight)。其中节点wip包含等待加工、加工中、等待运输的产品数量。

4. **处理分支情况：** 如果某个下游节点对应多条剩余路线（因为存在分支），应计算每条路线的在制品总量，然后保留最小的。

5. **选择最优路线：** 选择计算得到的其中当前在制品总量最少的一条剩余路线。

6. **处理相同情况：** 如果多条路线的在制品总量相同，则选择距离最近的下游节点。

> **说明：** 此策略旨在平衡各条生产线的负载，避免某些路线过度拥挤而其他路线空闲的情况，从而提高整体生产效率。

---

## 动态平衡算法(设备利用率)

当某个产品从一个节点出发时，按如下步骤执行：

1. **检测下游节点数量：** 检测当前节点的允许加工本产品的直接下游节点数量。如果只有1个则不做任何额外处理，直接选择该节点；如果大于1个，则进入下一步。

2. **获取剩余路线：** 从当前产品的产品工艺路线中，获取分别从当前节点允许加工本产品的不同下游节点为起点的剩余路线。

3. **计算设备利用率：** 对于每条剩余路线，计算路线上所有设备的利用率。设备利用率计算公式为：利用率 = 设备累计加工时间 / 模拟已运行时间 × 100%。

4. **确定路线最高利用率：** 对于每条路线，找出该路线上所有设备中的最高利用率值。

5. **处理分支情况：** 如果某个下游节点对应多条剩余路线（因为存在分支），应计算每条路线的最高设备利用率，然后保留最小的。

6. **选择最优路线：** 选择其中最高设备利用率最低的一条路线。这样可以避免将产品发送到已经有高利用率设备的路线，实现负载均衡。

7. **处理相同情况：** 如果多条路线的最高设备利用率相同，则选择距离最近的下游节点。如果距离也相同，则随机选择。

> **说明：** 此策略通过关注路线上最繁忙的设备（瓶颈设备），避免进一步加重其负担，从而实现更均衡的设备利用率分布，减少某些设备过载而其他设备空闲的情况。

---

## 缓冲区控制逻辑

缓冲区是平衡上下游工序生产节拍的关键设备。其控制逻辑涉及入站运输、出站释放和容量管控三个方面。

### 1. 入站运输（上游产品进入缓冲区）

当缓冲区的上游加工站有产品完成加工并等待运输时，系统按以下规则将产品运入缓冲区：

1. **容量检查：** 如果缓冲区已达到最大容量，则不再接收新产品，上游产品继续在加工站等待运输。

2. **收集上游设备：** 找出所有直接连接到缓冲区的上游加工站，且这些加工站当前有等待运输的产品。

3. **FIFO排序：** 对每个上游加工站中等待运输的产品，按到达该站的时间（`arrive_time_s`）升序排列，优先运输最早到达的产品。

4. **逐站运输：** 遍历所有上游加工站，每个站运输排序后的第一个等待运输产品到缓冲区。每运入一个产品，缓冲区当前库存加1，若达到容量上限则停止。

5. **触发时机：** 以下事件会触发入站运输检查：
   - 缓冲区的下游加工站完成加工时（`handle_process_complete`）
   - 缓冲区向下游释放产品后（`try_release_from_buffer`）

### 2. 出站释放（缓冲区产品送往下游）

当缓冲区有库存时，系统按以下规则将产品释放到下游：

1. **下游需求判断：** 检查缓冲区的所有下游设备（仅限加工站、装配站、终点），如果某一下游设备的有效在制品数（WIP + 在途 + 加工中）≤ 2，则认为该下游设备需要产品，允许释放。

2. **FIFO出库：** 按产品进入缓冲区的先后顺序，优先释放最早进入的产品。

3. **下游选择：** 按基础规则（产品匹配 → 优先空闲 → 距离最近）选择下游目标设备，且仅允许运往加工站、装配站或终点。

4. **触发时机：** 以下事件会触发出站释放检查：
   - 缓冲区的上游加工站完成加工时
   - 缓冲区收到上游运来的产品时（`handle_transport_complete`）
   - 缓冲区的下游加工站完成加工时

5. **级联操作：** 出站释放成功后，会立即检查缓冲区容量并尝试从上游继续运入产品（`check_buffer_capacity` → `try_transport_from_upstream_to_buffer`），形成"释放 → 补充"的循环。

### 3. 容量管控（投料控制）

缓冲区通过监控潜在库存来控制上游起点的投料节奏，防止缓冲区溢出：

1. **潜在库存计算：** 潜在库存 = 缓冲区当前库存 + 所有上游加工站的等待运输产品数之和。

2. **投料暂停：** 当潜在库存 ≥ 缓冲区最大容量时，缓冲区上游路径对应的所有起点暂停投料。

3. **投料恢复：** 当潜在库存 < 缓冲区最大容量时，恢复已暂停起点的投料（但受固定产量模式等其他规则约束的起点除外）。

4. **上游起点查找：** 系统会沿缓冲区的上游路径回溯，找到所有直接或间接向缓冲区供货的起点节点。

5. **触发时机：** 以下事件会触发容量管控检查：
   - 缓冲区向下游释放产品后
   - 缓冲区收到上游运来的产品后
   - 缓冲区的上游加工站完成加工后

### 4. 运输顺序保证

为确保产品运输的FIFO顺序正确，系统在以下环节严格执行排序：

1. **加工完成时：** 产品加工完成后状态变为"等待运输"（`WaitingForTransport`），但不会立即设置离开时间，离开时间仅在产品真正开始运输时记录。

2. **运输启动时：** `start_transport` 函数收集当前设备上所有等待运输的产品，统一按到达时间排序后按运输容量取前N个，确保先到的产品优先运输，不会因刚完成的产品插队而打乱顺序。

3. **等待队列启动时：** `try_start_pending_transport` 从运输连线的等待队列中按FIFO顺序取出产品启动运输。

### 5. 加工记录中的等待数统计

加工站的详细记录中包含以下两个统计指标：

| 指标 | 含义 |
|------|------|
| 开始时等待加工数 | 该产品开始加工时，当前设备上等待加工的产品数量 |
| 开始时等待运输数 | 该产品开始加工时，当前设备上等待运输的产品数量 |

这两个指标通过直接统计过程产品的状态得出，而非依赖设备计数器，确保数据的准确性。

---

## 仓库与临时堆场控制逻辑

仓库和临时堆场是用于存储产品的设备，其控制逻辑涉及入库处理、出库释放和投放模式三个方面。

### 1. 入库处理（产品进入仓库/临时堆场）

当产品运输到达仓库或临时堆场时，按以下规则处理：

1. **进入等待入库队列：** 产品首先进入等待入库队列（`waiting_entry_queue`），记录产品ID、产品编码和到达时间。

2. **状态变更：** 产品状态变为"等待入库"（`WaitingForStorage`），表示产品已到达但尚未正式入库。

3. **容量检查：** 系统循环检查当前库存是否小于容量上限：
   - **仓库：** 检查当前库存是否小于设定的容量
   - **临时堆场：** 无容量限制，始终可以入库

4. **正式入库：** 当有空位时，从等待入库队列头部取出产品（FIFO）：
   - 库存数量 +1
   - 更新最大库存记录
   - 产品状态变为"已存储"（`Stored`）
   - 记录入库事件（时间、类型、当前库存、容量、产品ID、到达时间）

5. **最大等待入库数统计：** 每次有产品进入等待入库队列时，更新最大等待入库数记录。

### 2. 出库释放（产品从仓库/临时堆场运出）

当仓库或临时堆场有库存时，按以下规则将产品释放到下游：

#### 2.1 投放模式

| 模式 | 说明 |
|------|------|
| 等待空闲 | 仅当所有下游设备都空闲时才释放产品（默认模式） |
| 立即投放 | 只要有库存就立即释放产品 |

#### 2.2 出库流程

1. **投放模式检查：** 根据投放模式判断是否可以出库：
   - `Immediate`（立即投放）：直接进入下一步
   - `WaitForIdle`（等待空闲）：检查所有直接下游设备是否都空闲

2. **下游连接选择：** 使用基础规则选择下游连接：
   - 产品匹配：下游设备必须能处理当前产品
   - 优先空闲：优先选择空闲的下游设备
   - 距离最近：如果多个设备条件相同，选择距离最近的

3. **下游设备类型检查：** 仅允许运往以下类型的下游设备：
   - 加工站（Station）
   - 装配站（AssemblyStation）
   - 终点（EndNode）
   
   > **注意：** 仓库/临时堆场不能直接运往另一个仓库或缓冲区。

4. **运输路径检查：** 检查运输连线是否可用（连续运输模式检查并发数，离散运输模式检查小车可用性）。

5. **出库数量计算：** 出库数量 = min(当前库存, 运输容量, 下游剩余容量)

6. **产品出库：** 按FIFO顺序从库存中取出产品：
   - 库存数量 -1
   - 产品状态变为"等待运输"（`WaitingForTransport`）
   - 记录出库事件

7. **启动运输：** 调用运输流程将产品送往下游设备。

### 3. 触发时机

以下事件会触发仓库/临时堆场的出库检查：

| 触发事件 | 说明 |
|---------|------|
| 产品入库后 | 有新产品入库时检查是否可以出库 |
| 运输完成后 | 产品运出后检查是否还有产品可以出库 |
| 下游设备加工完成 | 下游设备空闲时检查是否可以出库 |
| 运输路径可用 | 运输小车返回或并发数释放时检查 |

### 4. 与缓冲区的区别

| 特性 | 仓库/临时堆场 | 缓冲区 |
|------|--------------|--------|
| 容量限制 | 仓库有容量限制，临时堆场无限制 | 有容量限制 |
| 投放模式 | 可选"等待空闲"或"立即投放" | 固定为下游WIP≤2时释放 |
| 下游设备限制 | 仅限加工站、装配站、终点 | 加工站、装配站、终点 |
| 投料控制 | 无 | 有（控制上游起点投料） |
| 入库队列 | 有等待入库队列 | 直接入库 |

### 5. 统计指标

仓库和临时堆场的详细记录包含以下统计信息：

| 指标 | 说明 |
|------|------|
| 最终暂存量 | 模拟结束时的库存数量 |
| 最大暂存量 | 模拟过程中库存数量的最大值 |
| 最大等待入库数 | 模拟过程中等待入库队列长度的最大值 |
| 最大利用率 | 最大暂存量 / 容量 × 100%（仅仓库） |
| 变化记录数 | 入库和出库事件的总数 |

---

## 多上游工作站的处理逻辑

场景 ： 起点A → 工作站1 → 装配站 → 终点 ， 起点B → 工作站2 → 装配站 → 终点

1. 收集 ：从所有上游过程产品中收集 node_visits ，按 node_id 去重（同一节点只保留一条记录）
2. 追加 ：加上装配站本身的访问记录
3. 排序 ：按 arrive_time_s 升序排序，保证时间顺序正确
排序后 node_visits 类似： [起点A(0s), 起点B(1s), 工作站1(5s), 工作站2(6s), 装配站(10s), 终点(15s)]

calculate_product_avg_process_times 用 skip(1) 跳过第一个节点（起点）后，找到第一个工作站（工作站1，最早到达的），计算从工作站1到达时间到终点到达时间的差值作为处理时长。这符合"从产品首次进入加工环节到完成"的语义。

---

## 加工制品选择策略

当设备完成当前制品加工后，需要从等待加工队列中选择下一个加工制品时，按以下策略执行。该策略适用于工作站、装配站和拆解站，在模拟运行前设置，运行中不可更改。

### 1. 先到先生产（默认策略）

按产品到达设备的时间先后顺序选择，先到达的产品优先加工。

**选择步骤：**
1. 收集当前设备等待加工队列中的所有产品
2. 按到达时间（`arrive_time_s`）升序排列
3. 选择到达时间最早的产品

### 2. 同类优先兼顾工具

优先选择与上一次加工产品编码相同的产品；若无同类产品，则优先选择工具完全相同的产品；若工具也不完全相同，则选择相同工具数量最多的产品。

**选择步骤：**
1. **同类优先：** 从候选产品中筛选与上一次加工产品编码（`product_code`）相同的产品。若有，从中按到达时间最早的选择
2. **工具完全相同：** 若无同类产品，筛选工具编码集合与上一次加工完全一致的产品（忽略工具安装时间数字）。若有，从中按到达时间最早的选择
3. **最多相同工具：** 若无工具完全相同的产品，计算每个候选产品与上一次加工的相同工具编码数量，选择相同工具数量最多的产品。若有多个，按到达时间最早的选择
4. **兜底：** 若以上条件均无法筛选出产品，按到达时间最早的选择

> **说明：** "工具完全相同"是指工具编码集合完全一致，忽略工具后面的安装时间数字。例如 {T1: 2, T2: 1} 和 {T1: 1, T2: 2} 算工具完全相同。"最多相同工具"按相同工具编码的数量计，例如 {T1, T2} 和 {T1, T2, T3} 的相同工具数量为2（T1和T2）。

### 3. 同工具优先

优先选择工具编码集合与上一次加工完全相同的产品；若工具不完全相同，则选择相同工具数量最多的产品。

**选择步骤：**
1. **工具完全相同：** 从候选产品中筛选工具编码集合与上一次加工完全一致的产品（忽略工具安装时间数字）。若有，从中按到达时间最早的选择
2. **最多相同工具：** 若无工具完全相同的产品，计算每个候选产品与上一次加工的相同工具编码数量，选择相同工具数量最多的产品。若有多个，按到达时间最早的选择
3. **兜底：** 若以上条件均无法筛选出产品，按到达时间最早的选择

### 4. 首次加工退化

如果设备是第一次加工（没有上一次加工记录），则"同类优先兼顾工具"和"同工具优先"策略均退化为"先到先生产"。

### 5. 产品优先级

当勾选"考虑产品优先级"时，在选择策略执行前先进行优先级筛选：

1. **筛选最高优先级：** 从候选产品中找出设置了优先级且优先级数值最小（最高优先级）的产品。优先级数值1为最高，5为最低
2. **保留最高优先级产品：** 仅保留优先级等于最高优先级的产品作为候选。未设置优先级的产品不参与优先级比较
3. **策略选择：** 在筛选后的候选产品中，按当前选择策略（先到先生产/同类优先兼顾工具/同工具优先）选择具体的制品
4. **无优先级产品：** 如果所有候选产品均未设置优先级，则不进行筛选，所有产品均参与策略选择

> **说明：** 产品优先级为可选属性，默认为"未设置"。在产品管理界面中可设置1-5级优先级。

### 6. 各设备类型的应用

| 设备类型 | 选择对象 | 说明 |
|---------|---------|------|
| 工作站 | 等待加工队列中的过程产品 | 直接从等待加工的过程产品中选择 |
| 装配站 | 可装配的成品编码 | 先检查各成品的组件是否齐全，从组件齐全的成品中按策略选择 |
| 拆解站 | 可拆解的待拆解品编码 | 从有待拆解产品的编码中按策略选择 |

---

## 装配站加工规则

装配站将多个上游设备的产品组装成一个成品，其加工逻辑涉及组件收集、成品选择和装配执行三个方面。

### 1. 组件收集

当上游产品运输到达装配站时：

1. **产品入库：** 产品按产品编码分类存入装配站的在制品库（`assembly_wip`），每个产品编码对应一个过程产品ID列表
2. **状态变更：** 产品状态变为"等待加工"（`WaitingForProcessing`）
3. **触发检查：** 每次有新产品入库时，触发装配站是否可以开始加工的检查

### 2. 成品选择

装配站开始加工前，需要从可装配的成品中选择一个进行装配：

1. **组件齐全检查：** 遍历装配站设置的所有装配成品（`assembly_products`），检查每种成品所需的上游组件是否齐全。组件需求由`product_upstream_requirements`定义，包括组件编码和所需数量
2. **产量模式过滤：** 如果处于固定产量模式，且某成品已达到目标产量，则该成品不参与选择
3. **策略选择：** 从组件齐全的成品中，按"加工制品选择策略"选择一个成品进行装配

### 3. 装配执行

选定成品后，执行装配过程：

1. **消耗组件：** 从装配站在制品库中取出所需数量的组件过程产品，被消耗的产品状态变为"已消耗"（`Consumed``）
2. **工具切换：** 如果当前成品所需工具与上一次加工不同，先执行工具切换（切换时间计入总加工时间）
3. **生成成品：** 创建一个新的过程产品，产品编码为装配成品编码，状态为"加工中"（`Processing`）
4. **加工计时：** 根据加工时间分布计算本次装配耗时，安排加工完成事件

### 4. 装配完成

装配完成时：

1. **状态更新：** 装配站变为空闲，成品过程产品状态变为"等待运输"（`WaitingForTransport`）
2. **运输启动：** 按下游选择规则将成品运往下游设备
3. **继续装配：** 检查是否还有可装配的成品，若有则立即开始下一轮装配

### 5. 多上游工作站的处理

当装配站有多个上游工作站时，成品过程产品的工艺路线记录（`node_visits`）按如下规则合并：

1. **收集：** 从所有被消耗的上游过程产品中收集 `node_visits`，按 `node_id` 去重（同一节点只保留一条记录）
2. **追加：** 加上装配站本身的访问记录
3. **排序：** 按 `arrive_time_s` 升序排序，保证时间顺序正确

---

## 拆解站加工规则

拆解站将一个产品拆解为多个子产品，其加工逻辑涉及待拆解品选择、拆解执行和拆解产物处理三个方面。

### 1. 待拆解品入库

当上游产品运输到达拆解站时：

1. **产品入库：** 产品按产品编码分类存入拆解站的在制品库（`disassembly_wip`），每个产品编码对应一个过程产品ID列表
2. **状态变更：** 产品状态变为"等待加工"（`WaitingForProcessing`）
3. **触发检查：** 每次有新产品入库时，触发拆解站是否可以开始加工的检查

### 2. 待拆解品选择

拆解站开始加工前，需要从可拆解的产品中选择一个进行拆解：

1. **可用性检查：** 遍历拆解站设置的所有待拆解品（`items_to_disassemble`），检查每种待拆解品在在制品库中是否有库存
2. **策略选择：** 从有待拆解产品的编码中，按"加工制品选择策略"选择一个待拆解品进行拆解
3. **取出产品：** 从在制品库中取出该编码下最早到达的过程产品

### 3. 拆解执行

选定待拆解品后，执行拆解过程：

1. **消耗原料：** 被拆解的产品状态变为"已消耗"（`Consumed`），记录离开时间
2. **工具切换：** 如果当前待拆解品所需工具与上一次加工不同，先执行工具切换（切换时间计入总加工时间）
3. **生成拆解产物：** 根据`product_disassembly_requirements`定义的拆解产物清单，为每种产物创建相应数量的新过程产品。新产品的产品编码为拆解产物编码，状态为"加工中"（`Processing`），当前节点为拆解站
4. **加工计时：** 根据加工时间分布计算本次拆解耗时，安排加工完成事件

### 4. 拆解完成

拆解完成时：

1. **状态更新：** 拆解站变为空闲，所有拆解产物状态变为"等待运输"（`WaitingForTransport`）
2. **运输启动：** 按下游选择规则将各拆解产物分别运往下游设备。每个拆解产物独立选择下游路径
3. **继续拆解：** 检查是否还有可拆解的产品，若有则立即开始下一轮拆解

### 5. 拆解产物与工艺路线

- 拆解产物的工艺路线记录（`node_visits`）从拆解站开始，不继承被拆解产品的历史路线
- 拆解产物的产品编码和颜色由拆解产物定义决定，与被拆解产品无关
- 拆解产物的下游路径按其自身产品编码选择

---

## 多仓库选择优先级

当产品需要运往仓库/缓冲区，且存在多个可选的仓库/缓冲区时，系统按优先级规则逐级筛选，直到选出唯一的仓库。

### 1. 基本筛选

1. **容量过滤：** 首先筛选出有剩余容量的仓库/缓冲区。如果所有仓库/缓冲区都无剩余容量，则从全部候选中继续选择
2. **唯一候选：** 如果筛选后仅剩1个候选，直接选择该仓库/缓冲区

### 2. 逐级优先级筛选

系统按用户设置的优先级列表顺序，逐级对候选仓库/缓冲区进行评分和筛选：

1. **评分：** 根据当前优先级规则，为每个候选仓库/缓冲区计算评分
2. **筛选：** 找出评分最优（最低）的仓库/缓冲区。如果有多个评分相同且最优，则保留这些候选进入下一级筛选
3. **终止：** 如果某一级筛选后仅剩1个候选，直接选择该仓库/缓冲区；否则继续下一级优先级筛选
4. **兜底：** 如果所有优先级规则筛选完毕仍有多个候选，按仓库ID字典序选择第一个

### 3. 优先级规则说明

| 优先级规则 | 评分方式 | 说明 |
|-----------|---------|------|
| 距离最近 | 仓库与出发设备的欧几里得距离 | 优先选择距离最近的仓库 |
| 距离最远 | 仓库与出发设备的欧几里得距离（取负值） | 优先选择距离最远的仓库 |
| 利用率最低 | 当前库存 / 容量 | 优先选择利用率最低的仓库 |
| 利用率最高 | 当前库存 / 容量（取负值） | 优先选择利用率最高的仓库 |
| 按产品集中 | 当前仓库中与运输产品相同编码的存储数量（取负值） | 优先选择已存储同类产品最多的仓库 |
| 按产品分散 | 当前仓库中与运输产品相同编码的存储数量 | 优先选择已存储同类产品最少的仓库 |
| 等待入库最少 | 当前仓库等待入库队列长度 | 优先选择等待入库数量最少的仓库 |

### 4. 冲突规则

以下规则对互为冲突，不可同时选择：

| 规则A | 规则B | 冲突原因 |
|-------|-------|---------|
| 距离最近 | 距离最远 | 方向相反 |
| 利用率最低 | 利用率最高 | 方向相反 |
| 按产品集中 | 按产品分散 | 方向相反 |

### 5. 使用示例

假设有3个仓库，优先级设置为：1.利用率最低 → 2.距离最近

1. 第一级按"利用率最低"评分，假设仓库A利用率30%、仓库B利用率50%、仓库C利用率30%
2. 仓库A和C评分相同且最优，进入下一级
3. 第二级按"距离最近"评分，假设仓库A距离100、仓库C距离50
4. 仓库C距离最近，选择仓库C

---

## AI 自动优化可调参数

AI 自动优化功能在迭代优化过程中，可以调整以下配置参数。AI 不能修改任何设备的加工时间参数，加工时间是物理约束。

### 1. 全局策略参数

| 参数 | 中文名称 | 可选值 | 说明 |
|------|---------|--------|------|
| resource_selection_rule | 资源选择规则 | basic、min_wip_dynamic、min_utilrate_dynamic | 控制设备或起点选择下游节点的规则。basic为基础规则，min_wip_dynamic为最小在制品动态平衡，min_utilrate_dynamic为最低利用率动态平衡 |
| product_selection_strategy | 加工制品选择策略 | first_come_first_served、same_type_priority_with_tool、same_tool_priority | 控制设备从等待加工队列中选择下一个制品的策略。first_come_first_served为先到先生产，same_type_priority_with_tool为同类优先兼顾工具，same_tool_priority为同工具优先 |
| consider_product_priority | 是否考虑产品优先级 | true、false | 是否在选择制品时先按产品优先级筛选 |
| warehouse_selection_priorities | 仓库选择优先级 | nearest_distance、farthest_distance、lowest_utilization、highest_utilization、product_concentrated、product_dispersed、least_waiting_entry | 仓库选择优先级列表，按优先级从高到低排列。互斥规则：距离最近与距离最远互斥、利用率最低与利用率最高互斥、按产品集中与按产品分散互斥 |

### 2. 设备级参数

| 参数 | 中文名称 | 适用设备 | 可选值/范围 | 说明 |
|------|---------|---------|------------|------|
| release_mode | 投放模式 | 仓库、临时堆场 | immediate、wait_for_idle | 控制仓储设备向下游投放产品的方式。immediate为立即投放，wait_for_idle为等待下游设备空闲后投放 |
| max_capacity | 缓冲区容量 | 缓冲区 | 正整数 | 调整缓冲区的最大存储数量 |
| product_priority | 产品优先级 | 产品 | 1-5的整数或null | 1为最高优先级，5为最低优先级，null表示无优先级 |

### 3. 结构性调整

| 操作 | 中文名称 | 参数 | 说明 |
|------|---------|------|------|
| add_buffer | 添加缓冲区 | upstream_device_id（上游设备ID）、downstream_device_id（下游瓶颈设备ID）、capacity（缓冲区容量）、product_code（产品编码） | 在瓶颈设备前添加缓冲区，平衡上下游生产节拍。系统会自动创建缓冲区设备并修改上下游连接关系 |
| clone_device | 复制瓶颈设备 | device_id（要复制的设备ID）、count（复制数量，1-3） | 复制瓶颈加工节点以增加产能。系统会自动创建新设备并复制原设备的上下游连接 |

### 4. 利用率数据说明

AI 优化报告中所有利用率数值已经是百分比形式（范围0-100），不需要再乘以100。例如报告中显示利用率45.2，含义就是45.2%。

### 5. AI 优化 JSON 格式

AI 返回的配置变更建议格式如下：

```json
{
  "can_optimize": true或false,
  "should_continue": true或false,
  "changes": [
    {"type": "resource_selection_rule", "value": "min_wip_dynamic"},
    {"type": "product_selection_strategy", "value": "same_tool_priority"},
    {"type": "consider_product_priority", "value": true},
    {"type": "warehouse_selection_priorities", "value": ["nearest_distance", "lowest_utilization"]},
    {"type": "device_config", "device_id": "设备ID", "field": "release_mode", "value": "immediate"},
    {"type": "product_priority", "device_id": "产品编码", "field": "priority", "value": 1},
    {"type": "add_buffer", "value": {"upstream_device_id": "上游设备ID", "downstream_device_id": "下游瓶颈设备ID", "capacity": 10, "product_code": "产品编码"}},
    {"type": "clone_device", "value": {"device_id": "要复制的设备ID", "count": 1}}
  ],
  "reasoning": "详细的分析说明"
}
```

字段说明：
- **can_optimize**：根据当前数据和可调参数，判断是否还有优化空间
- **should_continue**：是否建议继续下一轮优化迭代
- **changes**：具体的配置调整列表，每项包含 type 和 value
- **reasoning**：详细的分析说明（使用中文）
