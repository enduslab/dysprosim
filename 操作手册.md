# DysProSim 操作手册

**版本：** v1.4.1

---

## 目录

1. [软件界面概览](#1-软件界面概览)
2. [菜单栏操作](#2-菜单栏操作)
3. [工具箱使用](#3-工具箱使用)
4. [画布操作](#4-画布操作)
5. [产品管理](#5-产品管理)
6. [物料管理](#6-物料管理)
7. [节点创建与配置](#7-节点创建与配置)
8. [连线创建与配置](#8-连线创建与配置)
9. [模拟运行](#9-模拟运行)
10. [统计分析](#10-统计分析)
11. [AI 分析](#11-ai-分析)
12. [AI 自动优化](#12-ai-自动优化)
13. [文件操作](#13-文件操作)
14. [快捷键参考](#14-快捷键参考)

---

## 1. 软件界面概览

软件界面由以下主要区域组成：

| 区域 | 位置 | 功能 |
|------|------|------|
| 菜单栏 | 顶部 | 文件、编辑、视图、设置、帮助等菜单 |
| 工具箱 | 左侧 | 选择工具、设备创建、连线创建等 |
| 画布 | 中央 | 模型编辑区域 |
| 属性面板 | 右侧 | 显示和编辑选中对象的属性 |
| 状态栏 | 底部 | 显示当前状态信息 |

---

## 2. 菜单栏操作

### 2.1 文件菜单

#### 新建画布
1. 点击菜单栏 **「文件」**
2. 选择 **「新建」**
3. 如当前画布有内容，系统会询问是否保存
4. 选择保存或不保存后，画布将被清空

#### 打开文件
1. 点击菜单栏 **「文件」**
2. 选择 **「打开」**
3. 在弹出的文件选择对话框中，选择 `.json` 格式的布局文件
4. 点击「打开」按钮

#### 保存文件
1. 点击菜单栏 **「文件」**
2. 选择 **「保存」** 或 **「另存为」**
3. 选择保存位置并输入文件名
4. 点击「保存」按钮

#### 导出图片
1. 点击菜单栏 **「文件」**
2. 选择 **「导出图片」**
3. 选择保存位置并输入文件名
4. 图片将以 PNG 格式保存

### 2.2 编辑菜单

#### 删除选中对象
1. 选中要删除的设备或连线
2. 点击菜单栏 **「编辑」**
3. 选择 **「删除」**
4. 或直接按 `Delete` 键

#### 清空画布
1. 点击菜单栏 **「编辑」**
2. 选择 **「清空画布」**
3. 确认清空操作

### 2.3 视图菜单

#### 缩放操作
1. 点击菜单栏 **「视图」**
2. 选择 **「放大」** 或 **「缩小」**
3. 或使用鼠标滚轮进行缩放

#### 重置视图
1. 点击菜单栏 **「视图」**
2. 选择 **「重置视图」**
3. 画布将恢复到初始缩放和位置

### 2.4 设置菜单

#### 画布设置
1. 点击菜单栏 **「设置」**
2. 选择 **「画布设置」**
3. 在弹出的对话框中设置：
   - 画布宽度（毫米）
   - 画布高度（毫米）
4. 点击「确定」保存设置

#### 产品管理
1. 点击菜单栏 **「设置」**
2. 选择 **「产品管理」**
3. 在弹出的对话框中进行产品配置

#### 物料管理
1. 点击菜单栏 **「设置」**
2. 选择 **「物料管理」**
3. 在弹出的对话框中进行物料配置

#### 工具管理
1. 点击菜单栏 **「设置」**
2. 选择 **「工具管理」**
3. 在弹出的对话框中进行工具配置

**工具配置说明：**
- **工具编码：** 工具的唯一标识符
- **工具名称：** 工具的显示名称

**工具管理操作：**
- 点击 **「新建工具」** 创建新工具
- 点击 **「编辑」** 修改已有工具
- 点击 **「删除」** 删除工具

**注意：** 工具的安装时间需要在加工站的产品配置中单独设置，因为同一工具在不同产品加工时可能需要不同的安装时间。

### 2.5 帮助菜单

#### 关于
1. 点击菜单栏 **「帮助」**
2. 选择 **「关于」**
3. 查看软件版本、开发商信息和官网链接

---

## 3. 工具箱使用

工具箱位于界面左侧，包含以下工具：

### 3.1 选择工具

**图标：** 箭头指针

**使用方法：**
1. 点击选择工具图标
2. 在画布上点击设备可选中单个设备
3. 按住 `Ctrl` 键点击可多选设备
4. 在画布空白处拖动可框选多个设备

### 3.2 平移工具

**图标：** 四向箭头

**使用方法：**
1. 点击平移工具图标
2. 在画布上拖动可移动视图位置
3. 也可在任何模式下按住鼠标中键拖动

### 3.3 设备创建工具

#### 工作站（矩形设备）
1. 点击工具箱中的矩形图标
2. 在画布上点击要创建设备的位置
3. 设备将以默认尺寸创建

#### 工作站（圆形设备）
1. 点击工具箱中的圆形图标
2. 在画布上点击要创建设备的位置

#### 起点
**图标：** 绿色圆形带播放符号

1. 点击起点图标
2. 在画布上点击创建起点
3. 每个模型至少需要一个起点

#### 终点
**图标：** 紫色菱形

1. 点击终点图标
2. 在画布上点击创建终点
3. 每个模型至少需要一个终点

### 3.4 连线创建工具

点击连线图标展开连线类型选择：

| 类型 | 说明 |
|------|------|
| 直线 | 两点之间直接连接 |
| 肘形连线 | 自动生成带拐点的折线，可拖动控制点调整拐点位置 |
| 曲线 | 平滑的贝塞尔曲线 |
| 手动折线 | 用户自定义拐点的折线 |

**创建连线：**
1. 选择连线类型
2. 将鼠标移到源设备的锚点上（设备边缘的小圆点）
3. 按住鼠标左键拖动到目标设备的锚点
4. 释放鼠标完成连线

### 3.5 仓储设备

点击仓储图标展开选择：

#### 仓库
1. 点击仓库图标
2. 在画布上点击创建仓库
3. 仓库具有固定容量限制

#### 临时堆场
1. 点击临时堆场图标
2. 在画布上点击创建临时堆场
3. 临时堆场无容量限制

### 3.6 缓冲区

**图标：** 分隔的矩形

1. 点击缓冲区图标
2. 在画布上点击创建缓冲区

### 3.7 车间

**图标：** 带虚线分隔的矩形

1. 点击车间图标
2. 在画布上点击创建车间
3. 可将设备、仓库、缓冲区放在车间内，移动车间时会同时移动车间内的设备
4. 设备、仓库、缓冲区被放在车间内后，会有这些变化：a. 车间内的节点移动时将不能超出车间边界，b. 节点的"所属车间"属性将被自动设置为车间名称，可通过调整上下左右边距修改节点相对于车间四边的距离

### 3.8 装配站

**图标：** 梯形（底边较宽）

1. 点击装配站图标
2. 在画布上点击创建装配站
3. 装配站用于将多个上游设备的产品组装成一个产品
4. 需要连接多个上游节点，并设置每个上游节点的来料需求量

### 3.9 拆解站

**图标：** 翻转梯形（顶边较宽）

1. 点击拆解站图标
2. 在画布上点击创建拆解站
3. 拆解站用于将一个产品拆解成多种产品
4. 需要设置待拆解品和拆解产物及其产出数量

---

## 4. 画布操作

### 4.1 缩放画布

| 操作方式 | 说明 |
|---------|------|
| 鼠标滚轮 | 向上滚动放大，向下滚动缩小 |
| 菜单「视图」→「放大/缩小」 | 每次缩放 20% |


### 4.2 平移画布

| 操作方式 | 说明 |
|---------|------|
| 平移工具模式下拖动 | 移动视图位置 |
| 按住鼠标中键拖动 | 任何模式下都可使用 |

### 4.3 选择对象

| 操作方式 | 说明 |
|---------|------|
| 点击设备 | 选中单个设备 |
| 点击连线 | 选中单条连线 |
| `Shift + 点击` | 添加到选择集 |
| 框选 | 拖动选择框选中多个设备 |

### 4.4 移动设备

1. 在选择工具模式下
2. 点击选中设备
3. 拖动设备到目标位置
4. 多选后可同时移动多个设备

### 4.5 删除对象

1. 选中要删除的对象
2. 按 `Delete` 键
3. 或在属性面板点击删除按钮

---

## 5. 产品管理

### 5.1 打开产品管理

1. 点击菜单栏 **「设置」**
2. 选择 **「产品管理」**
3. 弹出产品管理对话框

### 5.2 添加产品

1. 点击 **「添加产品」** 按钮
2. 填写产品信息：
   - **编码**：产品唯一标识
   - **名称**：产品显示名称
   - **颜色**：在图表中显示的颜色
   - **优先级**：产品加工优先级，可选1~5级，数字越小优先级越高。不设置表示无优先级
3. 点击颜色块选择颜色
4. 点击「确定」保存

**产品优先级说明：**
- 优先级为可选设置，不设置时表示该产品无优先级
- 优先级范围为1~5，1为最高优先级，5为最低优先级
- 当在模拟控制面板中勾选「考虑产品优先级」时，系统会优先选择优先级更高的产品进行加工
- 优先级仅影响同一设备等待队列中不同产品之间的选择顺序


### 5.3 设置产品物料清单 (BOM)

1. 在产品列表中选中产品
2. 点击 **「编辑」** 按钮
3. 在弹出的编辑器中
4. 点击「添加原料」
5. 选择原料类型（已在原料管理中创建）
6. 输入每个产品消耗的原料数量
7. 点击「确定」保存

### 5.4 删除产品

1. 在产品列表中选中产品
2. 点击 **「删除」** 按钮
3. 确认删除操作

---

## 6. 物料管理

### 6.1 打开物料管理

1. 点击菜单栏 **「设置」**
2. 选择 **「物料管理」**
3. 弹出物料管理对话框

### 6.2 创建物料

1. 点击 **「添加物料」** 按钮
2. 填写物料信息：
   - **编码**：物料唯一标识
   - **名称**：物料显示名称
   - **单位**：计量单位（如：个、千克、米）
3. 点击「确定」保存

### 6.3 删除物料

1. 在物料列表中选中物料
2. 点击 **「删除」** 按钮
3. 确认删除操作

---

## 7. 设备创建与配置

### 7.1 起点 (StartNode)

**创建步骤：**
1. 在工具箱点击起点图标
2. 在画布上点击创建

**属性配置：**

| 属性 | 说明 | 设置方法 |
|------|------|---------|
| 名称 | 设备显示名称 | 在属性面板输入 |
| 产品 | 起点生成的产品 | 在「产品设置」标签选择 |
| 生成间隔 | 产品生成的时间间隔 | 输入秒数 |
| 初始数量 | 模拟开始时的初始产品数 | 输入数量 |
| 目标产量 | 固定产量模式下的投料目标 | 输入数量（固定产量模式时使用） |
| 投料设置 | 原料消耗配置 | 在「投料设置」标签配置 |

**目标产量说明：**
- 仅在「固定产量」模拟模式下生效
- 设置起点需要投料的产品数量
- 当投料数量达到目标产量后，起点停止投料
- 如果不设置或设置为0，表示不限量投料

**投料设置：**
1. 切换到「投料设置」标签
2. 点击「添加投料」
3. 选择原料类型
4. 设置每个产品消耗的原料数量

### 7.2 终点 (EndNode)

**创建步骤：**
1. 在工具箱点击终点图标
2. 在画布上点击创建

**属性配置：**

| 属性 | 说明 |
|------|------|
| 名称 | 设备显示名称 |
| 产品 | 接收的产品类型 |

### 7.3 设备 (Station)

**创建步骤：**
1. 在工具箱点击矩形或圆形图标
2. 在画布上点击创建

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 基础信息 | 宽度/高度 | 设备尺寸（毫米） |
| 产品设置 | 允许产品 | 选择可加工的产品 |
| 加工设置 | 加工时间 | 加工一个产品所需时间，支持时/分/秒输入 |

**设置加工产品：**
1. 切换到「产品设置」标签
2. 勾选允许加工的产品
3. 如果不勾选任何产品，则表示可加工所有产品

**设置加工时间：**
1. 切换到「加工设置」标签
2. 选择「进料规则」，"即来即产"表示产品生成后立即加工；"集齐后加工"用于有多个上游设备的情况，只有当所有上边设备加工的产品都到达最少一个时才会开始加工
3. 「分布类型」的选择影响设备加工时间的随机生成方法，可选择"正态分布"、"均匀分布"、"指数分布"、"三角分布"，然后根据出现的属性填写时间参数。如"正态分布"只填写平均值的话，则会默认使用标准差为 0 的正态分布。
4. 时间参数输入框支持时、分、秒三个字段分别输入，方便设置较长或较短的时间。例如输入"1时30分0秒"表示5400秒

**设置产品加工时间：**
1. 在「加工设置」标签中，可以为不同产品设置不同的加工时间
2. 点击「添加产品加工时间」按钮
3. 选择要设置的产品
4. 为该产品设置专门的加工时间和分布类型
5. 如果某个产品没有专门设置，则使用默认的加工时间
6. 可以添加多个产品的加工时间配置

**产品加工时间优先级：**
- 如果为某个产品专门设置了加工时间，则使用该设置
- 如果没有专门设置，则使用默认的加工时间
- 这样可以灵活地为不同产品设置不同的加工时长

**设置产品加工工具：**
1. 在「加工设置」标签中，展开产品配置
2. 在「加工工具」区域，勾选该产品需要的工具
3. 对于已选中的工具，可以设置该工具的安装时间（秒）
4. 安装时间同时也是卸载时间
5. 每个产品可选择多个工具，每个工具可单独设置安装时间

**工具切换时间计算规则：**
- **首次加工：** 如果是工作站的第一次加工，需要安装所有需要的工具，切换时间 = 所有工具安装时间之和
- **相同产品：** 如果本次加工的产品与上次相同，无需切换工具，切换时间 = 0
- **不同产品：** 如果本次加工的产品与上次不同，需要计算工具切换时间：
  - 卸载不再使用的工具（卸载时间 = 安装时间）
  - 安装新需要的工具
  - 切换时间 = 卸载工具时间 + 安装工具时间
- **示例：** 上次使用工具A(10s)、B(15s)，本次使用工具B(15s)、C(20s)
  - 需要卸载：工具A(10s)
  - 需要安装：工具C(20s)
  - 切换时间 = 10s + 20s = 30s（工具B已安装，无需切换）

**甘特图中的工具切换显示：**
- 工具切换时间在甘特图中以橙色色块显示
- 加工任务以产品颜色显示
- 鼠标悬停可查看详细信息

### 7.4 仓库 (Warehouse)

**创建步骤：**
1. 在工具箱展开仓储菜单
2. 点击仓库图标
3. 在画布上点击创建

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 允许产品 | 选择可存储的产品 |
| 仓库设置 | 容量 | 最大存储数量 |

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 允许产品 | 选择可存储的产品 |
| 仓库设置 | 容量 | 最大存储数量 |
| 仓库设置 | 投放模式 | 向下游投放所存储的产品的方式，默认为"等待空闲"，指当下游设备空闲时才会投放，可选为"立即投放"，指当仓库有产品时就会向下游运送 |

### 7.5 临时堆场 (TempStore)

**创建步骤：**
1. 在工具箱展开仓储菜单
2. 点击临时堆场图标
3. 在画布上点击创建

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 允许产品 | 选择可存储的产品 |
| 投放设置 | 投放模式 | 向下游投放所存储的产品的方式，默认为"等待空闲"，指当下游设备空闲时才会投放，可选为"立即投放"，指当仓库有产品时就会向下游运送 |

### 7.6 缓冲区 (Buffer)

**创建步骤：**
1. 在工具箱点击缓冲区图标
2. 在画布上点击创建

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 允许产品 | 选择可存储的产品 |
| 缓冲区设置 | 容量 | 最大存储数量 |

**缓冲区使用规则：**

缓冲区用于平衡上下游工序的生产节拍，当上游生产速度快于下游时，缓冲区暂存产品，避免上游阻塞；当下游需要产品时，缓冲区按先进先出原则释放产品。

**典型使用场景：**
- 上游加工站生产速度快于下游，需要暂存产品
- 多个上游加工站共用一个下游加工站，需要缓冲区汇聚产品
- 需要控制投料节奏，防止在制品过多

**缓冲区控制逻辑：**

1. **入站运输（上游产品进入缓冲区）：**
   - 缓冲区达到最大容量时不再接收新产品
   - 收集所有直接连接到缓冲区的上游加工站中等待运输的产品
   - 按到达时间FIFO排序，优先运输最早到达的产品
   - 遍历所有上游加工站，每个站运输排序后的第一个等待运输产品
   - 每运入一个产品，缓冲区当前库存加1，达到容量上限则停止

2. **出站释放（缓冲区产品送往下游）：**
   - 仅当下游设备（加工站、装配站、终点）的有效在制品数（WIP + 在途 + 加工中）≤ 2 时，才允许释放产品
   - 按产品进入缓冲区的先后顺序，优先释放最早进入的产品
   - 按基础规则（产品匹配 → 优先空闲 → 距离最近）选择下游目标设备
   - 出站释放成功后，会立即检查缓冲区容量并尝试从上游继续运入产品

3. **容量管控（投料控制）：**
   - 潜在库存 = 缓冲区当前库存 + 所有上游加工站的等待运输产品数之和
   - 当潜在库存 ≥ 缓冲区最大容量时，上游路径对应的所有起点暂停投料
   - 当潜在库存 < 缓冲区最大容量时，恢复已暂停起点的投料
   - 系统沿缓冲区的上游路径回溯，找到所有直接或间接向缓冲区供货的起点节点

### 7.7 车间 (Workshop)

**创建步骤：**
1. 在工具箱点击车间图标
2. 在画布上点击创建

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |

### 7.8 装配站 (AssemblyStation)

**创建步骤：**
1. 在工具箱点击装配站图标
2. 在画布上点击创建

**功能说明：**
装配站用于将多个上游设备的产品组装成一个产品。它接收来自多个上游节点的产品，当所需的上游产品都到达后，进行组装加工，产出最终产品。

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 组件 | 选择参与装配的上游产品 |
| 产品设置 | 装配成品 | 选择装配后产出的产品 |
| 加工设置 | 加工时间 | 装配一个产品所需时间，支持时/分/秒输入 |

**设置装配产品：**
1. 切换到「产品设置」标签
2. 在「组件」区域，勾选参与装配的上游产品（即从上游节点运入的产品）
3. 在「装配成品」区域，勾选装配完成后产出的产品
4. 每个装配成品可以设置独立的上游来料需求

**设置上游来料需求：**
1. 在「产品设置」标签中，展开已选装配成品的配置
2. 在「上游来料需求」区域，可以看到所有连接到装配站的上游节点
3. 为每个上游节点设置装配一个产品所需的来料数量
4. **重要：** 所有上游节点的需求量必须大于0，否则检查时会报错

**装配逻辑说明：**
- 装配站会等待所有上游节点的产品到达
- 当每个上游节点的产品数量都达到设定的需求量时，才开始装配
- 装配完成后，消耗所有组件产品，产出一个装配成品送往下游
- 例如：产品A需要上游节点1的2件产品和上游节点2的3件产品，则只有当节点1有2件、节点2有3件产品时，才会开始装配

**工艺路线说明：**
装配站涉及两种工艺路线类型：
- **组件→装配站：** 组件产品从起点到装配站的路线，到达装配站即为终点
- **装配站→终点：** 装配成品从装配站出发，经过下游节点到达终点的路线

**检查要求：**
在模拟前检查时，装配站需要满足以下条件：
1. 必须选择至少一个组件和一个装配成品
2. 对于每个装配成品，必须设置上游来料需求
3. 每个上游节点的需求量必须大于0
4. 如果检查不通过，将显示错误信息，无法开始模拟

**加工时间设置：**
与普通工作站相同，装配站支持：
- 为不同产品设置不同的加工时间
- 选择分布类型（正态分布、均匀分布、指数分布、三角分布）
- 设置工具及安装时间

### 7.9 拆解站 (DisassemblyStation)

**创建步骤：**
1. 在工具箱点击拆解站图标
2. 在画布上点击创建

**功能说明：**
拆解站用于将一个产品拆解成多种产品。它接收上游运来的待拆解品，经过拆解加工后，产出多种拆解产物送往下游。拆解站与装配站的功能相反：装配站是多合一，拆解站是一分多。

**属性配置：**

| 标签 | 属性 | 说明 |
|------|------|------|
| 基础信息 | 名称 | 设备显示名称 |
| 产品设置 | 待拆解品 | 选择被拆解的产品 |
| 产品设置 | 拆解产物 | 选择拆解后产出的产品 |
| 拆解设置 | 拆解时间 | 拆解一个产品所需时间，支持时/分/秒输入 |

**设置拆解产品：**
1. 切换到「产品设置」标签
2. 在「待拆解品」区域，勾选被拆解的产品（即从上游运入的产品）
3. 在「拆解产物」区域，勾选拆解后产出的产品

**设置拆解产物数量：**
1. 切换到「拆解设置」标签
2. 设置默认拆解时间（秒）和分布类型
3. 展开每个待拆解品的配置，可以设置：
   - **专属拆解时间：** 为不同待拆解品设置不同的拆解时间
   - **拆解工具：** 为不同待拆解品设置需要的工具及安装时间
   - **拆解产物数量：** 设置每消耗1个待拆解品可获得的各拆解产物的数量（单位：份）
4. **重要：** 每个待拆解品至少需要设置一个拆解产物的数量大于0

**拆解逻辑说明：**
- 拆解站接收上游运来的待拆解品
- 当有待拆解品到达且拆解站空闲时，开始拆解加工
- 拆解完成后，消耗1个待拆解品，根据设置的数量产出对应的拆解产物
- 产出的拆解产物作为新的过程产品送往下游，运输方式与普通加工站相同
- 例如：待拆解品A设置拆解产物B为2份、产物C为3份，则每消耗1个A，产出2个B和3个C

**工艺路线说明：**
拆解站涉及两种工艺路线类型：
- **待拆解品→拆解站：** 待拆解品从起点到拆解站的路线，到达拆解站即为终点
- **拆解站→下游：** 拆解产物从拆解站出发，经过下游节点到达终点的路线

**待拆解品可达性说明：**
待拆解品必须能从起点或上游拆解站获取，系统会沿上游路径追溯：
- 只经过起点、加工站、仓库、临时堆场、缓冲区和上游拆解站
- 不经过装配站（因为装配站的产出是装配成品，不是原始产品）
- 装配成品不能作为待拆解品使用

**检查要求：**
在模拟前检查时，拆解站需要满足以下条件：
1. 必须选择至少一个待拆解品
2. 必须选择至少一个拆解产物
3. 每个待拆解品必须设置至少一个拆解产物的数量
4. 每个拆解产物的产出数量必须大于0
5. 待拆解品必须能从起点或上游拆解站获取（可达性检查）
6. 装配成品不能作为待拆解品
7. 如果检查不通过，将显示错误信息，无法开始模拟

**加工时间设置：**
与普通工作站相同，拆解站支持：
- 为不同待拆解品设置不同的拆解时间
- 选择分布类型（正态分布、均匀分布、指数分布、三角分布）
- 设置工具及安装时间

**详细记录说明：**
拆解站的处理记录中，过程产品ID列会显示本次拆解产生的所有拆解产物的过程产品ID，以逗号分隔。

---

## 8. 连线创建与配置

### 8.1 创建连线

1. 在工具箱选择连线类型
2. 将鼠标移到源设备边缘的锚点上（设备边缘的小圆点）点击一下
3. 鼠标移动到目标设备的锚点，再点击一下. 完成连线
4. 连线既代表了节点之间的关联关系，也标明了上下游关系，同时也是节点之间的运输路线（起点到下游，节点到终点的连线除外），连线的长度就是运输路线长度

### 8.2 连线类型

| 类型 | 图标 | 适用场景 |
|------|------|---------|
| 直线 | — | 简单连接，无障碍物 |
| 肘形连线 | ⌐ | 自动生成折线，可拖动调整拐点 |
| 曲线 | ⌒ | 美观展示 |
| 手动折线 | ⌈ | 自定义路径 |

### 8.3 连线属性配置

选中连线后，在右侧属性面板配置：

**基础信息标签：**

| 属性 | 说明 |
|------|------|
| 名称 | 连线显示名称 |
| 长度 | 连线长度（米） ，自动根据画布上的位置计算|
| 起点 | 源设备名称 |
| 终点 | 目标设备名称 |

**运输设置标签：**

| 属性 | 说明 |
|------|------|
| 运输模式 | 连续运输 / 离散运输 |
| 运输速度 | 运输速度，支持秒/分/时三种时间单位，默认1 m/s |
| 无限制并发 | 连续运输模式下，是否不限制同时运输的产品数量（默认开启） |
| 最大并发数 | 连续运输模式下，关闭"无限制并发"后可设置路线可同时承载的最大产品数 |
| 小车数量 | 离散运输模式的小车数量 |
| 小车单次运量 | 每辆小车单次最大运输量 |

### 8.4 运输模式说明

**连续运输：**
- 传送带式的运输
- 适合流水线生产
- 默认不限制同时运输的产品数量；关闭"无限制并发"后可设置最大并发数

**离散运输：**
- 小车批量运输
- 需设置小车数量和单次运量
- 运输记录包含批次信息

---

## 9. 模拟运行

### 9.1 打开模拟控制面板

1. 模拟控制面板默认打开，在界面底部
2. 可点击菜单栏 **「模拟」**
3. 选择 **「隐藏/显示模拟控制面板」**，控制是否显示模拟控制面板

### 9.2 设置模拟参数

**模拟时长：**
1. 点击「设置」按钮，打开下方设置面板
2. 在「模拟时长」输入框输入数值
3. 在时间单位下拉框中选择时间单位：秒、分钟、小时、天
4. 默认为 3600 秒（1小时）
5. 选择不同的时间单位时，系统会自动换算。例如选择"小时"并输入8，等同于28800秒

**模拟速度：**
1. 拖动速度滑块
2. 可选范围：1x ~ 100x
3. 常用速度：1x、2x、5x、10x、50x、100x

**动态平衡算法：**
1. 选择算法类型：
   - **无**：使用基础规则
   - **动态平衡算法(在制品)**：根据在制品数量选择路线
   - **动态平衡算法(设备利用率)**：根据设备利用率选择路线

**利用率采样间隔：**
1. 在「利用率采样间隔」输入框输入秒数
2. 该参数控制模拟过程中设备利用率的采样频率
3. 默认为1秒，即每秒采样一次利用率
4. 较小的采样间隔可以更精确地反映利用率变化，但会增加计算开销
5. 较大的采样间隔可以减少计算开销，但可能遗漏短时间的利用率峰值

**模拟模式：**
1. 点击「模拟模式」下拉菜单选择模拟结束条件：
   - **固定时长**：按照设定的模拟时长运行，模拟到达设定时长后结束（默认模式）
   - **固定产量**：按照设定的目标产量运行，当所有起点达到目标产量且所有产品到达终点后结束
2. 选择「固定产量」模式时，需要在终点的属性中设置目标产量
3. 选择「固定产量」模式时，可设置「截止时间」，同样支持秒/分钟/小时/天的时间单位选择。截止时间到达后，无论是否达到目标产量，模拟都会结束

**多仓库选择优先级：**
当节点的下游有多个仓库时，系统按照优先级策略选择目标仓库。在设置面板中可配置4个优先等级，从"优先等级1"到"优先等级4"横向排列，系统按等级从高到低依次应用。

1. 点击「设置」按钮，打开下方设置面板
2. 在「多仓库选择优先级」区域，为每个优先等级选择策略
3. 优先级选项说明：

| 选项 | 说明 |
|------|------|
| 距离最近 | 仓库与当前节点的连线路线长度最短 |
| 距离最远 | 仓库与当前节点的连线路线长度最长 |
| 利用率最低 | 仓库利用率（当前暂存量/最大容量）最小 |
| 利用率最高 | 仓库利用率（当前暂存量/最大容量）最大 |
| 按产品集中 | 仓库中已库存的与当前运输产品类型相同的产品数量最多 |
| 按产品分散 | 仓库中已库存的与当前运输产品类型相同的产品数量最少 |
| 等待入库最少 | 仓库的等待入库队列中对象最少 |

4. 默认优先级组合为：优先等级1=距离最近，优先等级2=利用率最低，优先等级3=按产品集中，优先等级4=等待入库最少

**互斥规则：**
以下选项对不能同时出现在优先级选择中：
- 「距离最近」和「距离最远」
- 「利用率最低」和「利用率最高」
- 「按产品集中」和「按产品分散」

违反互斥规则时，模拟前检查将报错，无法开始模拟。

**仓库选择判断流程：**
当一个节点需要运输过程产品时，系统按以下流程选择目标仓库：
1. 首先判断下游允许存放该产品类型的仓库有几个，如果只有1个，直接运到该仓库
2. 如果有多个，筛选剩余容量不为0的仓库，如果只有1个，运到该仓库
3. 如果仍有多个，按优先等级1的策略筛选，如果只有1个最优，运到该仓库
4. 如果仍有多个，按优先等级2的策略筛选，以此类推
5. 如果4个优先等级都筛选后仍有多个，按仓库ID从低到高排列，选择第一个

**加工制品选择策略：**
当设备的等待加工队列中有多个产品时，系统按照设定的策略选择下一个加工的产品。在设置面板中可选择策略类型，并可勾选是否考虑产品优先级。

1. 点击「设置」按钮，打开下方设置面板
2. 在「加工制品选择策略」下拉菜单中选择策略
3. 可勾选「考虑产品优先级」复选框，启用产品优先级筛选

**策略选项说明：**

| 策略 | 说明 |
|------|------|
| 先到先生产 | 按产品到达等待队列的时间先后顺序，优先加工最早到达的产品 |
| 同类优先兼顾工具 | 优先选择与上一次加工产品编码相同的产品；如果有多条同类产品，优先选择工具完全相同的产品；工具不完全相同时，选择相同工具数量最多的产品；如果设备是第一次加工（无上一次记录），退化为"先到先生产" |
| 同工具优先 | 优先选择与上一次加工工具完全相同的产品（工具编码集合完全一致，忽略安装时间）；如果无完全相同工具的产品，选择相同工具数量最多的产品；如果设备是第一次加工（无上一次记录），退化为"先到先生产" |

**考虑产品优先级：**
勾选后，系统会先按产品优先级（1~5级，1为最高）筛选出最高优先级的产品，再在同级优先级的产品中按上述策略选择具体产品。未设置优先级的产品视为最低优先级。

**各站点适用说明：**
- **加工站**：策略适用于等待加工队列中的产品选择
- **装配站**：策略适用于装配成品的选择，"同类"和"同工具"指装配成品的编码/工具
- **拆解站**：策略适用于待拆解品的选择，"同类"和"同工具"指待拆解品的编码/工具

**工具相同判断规则：**
- **工具完全相同**：两个产品的工具编码集合完全一致（忽略安装时间数字）。例如 {T1: 2s, T2: 1s} 和 {T1: 1s, T2: 2s} 算工具完全相同
- **最多相同工具**：按相同工具编码的数量计算。例如 {T1, T2} 和 {T1, T2, T3}，相同工具数量为2（T1和T2）

### 9.3 运行模拟

**获取工艺路线：**
1. 点击 **「获取工艺路线」** 按钮
2. 获取模型中所有产品的工艺路线，所有选择的产品最少应有一条完整的工艺路线
3. 可点击 **「查看工艺路线」** 按钮，查看工艺路线

**开始模拟：**
1. 点击 **「开始」** 按钮
2. 模拟开始运行
3. 状态显示为「运行中」

**暂停模拟：**
1. 模拟运行中点击 **「暂停」** 按钮
2. 模拟暂停
3. 状态显示为「已暂停」

**继续模拟：**
1. 暂停状态下点击 **「继续」** 按钮
2. 模拟继续运行

**重置模拟：**
1. 点击 **「重置」** 按钮
2. 模拟状态清零
3. 所有设备恢复初始状态

### 9.4 查看模拟状态

模拟控制面板显示：

| 信息 | 说明 |
|------|------|
| 状态 | 空闲/运行中/已暂停/已完成 |
| 已运行时间 | 当前模拟已运行的时间 |
| 总时长 | 设置的模拟总时长 |
| 完成产品 | 已到达终点的产品数量 |
| 模拟速度 | 当前模拟速度 |

---

## 10. 统计分析

### 10.1 打开统计记录

1. 点击模拟控制面板的 **「保存模拟记录」**，可将本次模拟的结果保存进历史记录进行查看，没有保存时看不到模拟结果数据
2. 选择 **「查看历史记录」**
3. 弹出模拟统计记录窗口

### 10.2 查看记录列表

上方显示模拟记录列表：

| 列 | 说明 |
|------|------|
| 时长 | 模拟运行时长 |
| 完成产品 | 完成加工的产品总数 |
| 整体最大在制品数 | 整个模拟过程中，所有节点汇总的最大在制品数 |

### 10.3 查看详细统计

选中一条历史记录后，下方显示详细统计：

**图表展示：**

| 图表 | 说明 |
|------|------|
| 模拟选项 | 显示本次模拟的模拟模式、资源选择规则、多仓库选择优先级组合、加工制品选择策略 |
| 完成产品数 | 按产品种类显示完成数量 |
| 产品平均处理时长 | 按产品种类显示平均处理时长柱状图 |
| 原料消耗量 | 按原料种类显示消耗量 |
| 设备利用率 | 各设备的利用率百分比 |
| 设备加工数量 | 各设备加工各产品的数量 |
| 设备最大在制品 | 各设备的最大在制品数 |
| 仓库利用率 | 各仓库的最大利用率 |
| 路径利用率 | 各连线路径的利用率（不包含上游是起点的连线和下游是终点的连线） |
| 甘特图 | 设备加工时间线，显示各产品的加工时段 |

**颜色说明（仓库利用率）：**

| 利用率 | 颜色 |
|--------|------|
| ≥90% | 红色 |
| ≥70% | 橙色 |
| ≥50% | 蓝色 |
| <50% | 绿色 |

**仓库统计图表：**

| 图表 | 说明 |
|------|------|
| 仓库最大利用率 | 各仓库的最大利用率柱状图 |
| 仓库最大等待入库量和库存量 | 各仓库的最大库存量（蓝色）和最大等待入库量（红色）的分组柱状图 |

### 10.4 查看设备详细记录

1. 在统计页面点击「设备统计」标签
2. 选择要查看的设备
3. 显示该设备的详细加工记录：
   - 产品编码
   - 加工品序号
   - 任务类型（加工任务/工具切换）
   - 到达时间
   - 开始时间
   - 完成时间
   - 运出时间（产品真正开始运输的时间，若未运出则显示"-"）
   - 加工时长
   - 开始时等待加工数（该产品开始加工时，当前设备上等待加工的产品数量）
   - 开始时等待运输数（该产品开始加工时，当前设备上等待运输的产品数量）
   - 使用原料

**设备统计指标：**

| 指标 | 说明 |
|------|------|
| 最大等待加工数 | 模拟过程中设备等待加工产品数的最大值 |
| 最大等待运输数 | 模拟过程中设备等待运输产品数的最大值 |
| 平均加工时间 | 设备加工各产品的平均时间 |

### 10.5 查看仓库/缓冲区详细记录

1. 在统计页面点击「存储统计」标签
2. 选择要查看的仓库或缓冲区
3. 显示该存储设备的详细统计信息：
   - 最终暂存量
   - 最大暂存量
   - 最大等待入库数
   - 最大利用率
   - 变化记录数

**存储设备统计图表：**

| 图表 | 说明 |
|------|------|
| 库存利用率变化 | 存储设备利用率随时间的变化曲线 |
| 库存量与等待入库量变化 | 库存量（蓝色）和等待入库量（红色）随时间的变化曲线，每秒更新一次 |

**变化记录详情：**

| 字段 | 说明 |
|------|------|
| 时间 | 变化发生的时间 |
| 类型 | 入库/出库 |
| 当前暂存量 | 变化后的库存数量 |
| 最大容量 | 存储设备的最大容量 |
| 过程产品ID | 相关产品的唯一标识 |
| 到达时间 | 产品到达存储设备的时间 |
| 入库时间 | 产品实际入库的时间 |

### 10.6 查看连线详细记录

1. 在统计页面点击「连线统计」标签
2. 选择要查看的连线
3. 显示该连线的详细运输记录：
   - 运输批次（离散运输模式）
   - 过程产品ID
   - 产品编码
   - 运送序号
   - 开始时间
   - 完成时间
   - 用时

### 10.7 导出模拟记录

**导出PDF：**
1. 在模拟记录窗口中，选中一条记录
2. 点击 **「导出PDF」** 按钮
3. 在弹出的文件保存对话框中选择保存位置和文件名
4. 系统将模拟记录的完整统计信息导出为PDF文件，包含：
   - 模拟概要信息（含模拟模式、资源选择规则、多仓库选择优先级、加工制品选择策略）
   - 完成产品统计
   - 产品平均处理时长
   - 原料消耗统计
   - 设备利用率
   - 设备加工数量
   - 设备最大在制品
   - 仓库利用率
   - 路径利用率
   - 甘特图

**导出Excel：**
1. 在模拟记录窗口中，选中一条记录
2. 点击 **「导出Excel」** 按钮
3. 在弹出的文件保存对话框中选择保存位置和文件名
4. 系统将模拟记录的详细数据导出为Excel文件，包含：
   - 完成产品统计（含模拟模式、资源选择规则、多仓库选择优先级、加工制品选择策略）
   - 原料消耗统计
   - 各设备加工记录
   - 各连线运输记录
   - 仓库变化记录
   - 投料记录

### 10.8 查看甘特图

甘特图以时间线形式展示各设备的加工情况，直观显示生产过程。

**甘特图说明：**
- **横轴**：时间（分钟）
- **纵轴**：设备名称
- **色块**：表示连续加工某种产品的时段
- **色块内数字**：该时段内连续加工的产品数量
- **颜色**：不同产品使用不同颜色区分

**查看甘特图：**
1. 在统计页面找到「甘特图（设备加工时间线）」
2. 图表自动显示所有加工站的加工时间线
3. 每个色块代表一个连续加工时段
4. 鼠标悬停在色块上可查看详细信息

**甘特图合并规则：**
- 相同产品的连续加工记录会合并为一个色块
- 如果相同产品的两次加工间隔超过1秒，会显示为两个独立的色块
- 不同产品的加工记录始终显示为独立的色块

---

## 11. AI 分析

AI 分析功能利用大语言模型对模拟运行结果进行智能分析和对比，帮助用户快速发现生产瓶颈、识别潜在问题并获得优化建议。

### 11.1 打开 AI 分析

1. 在模拟控制面板中，点击 **「AI」** 按钮
2. 弹出 AI 分析窗口
3. 如果尚未保存任何模拟记录，该按钮为灰色不可点击状态，需先运行模拟并保存记录

**注意：** AI 分析窗口支持拖动，可拖动标题栏移动窗口位置。

### 11.2 选择分析记录

在 AI 分析窗口的「选择分析」标签页中：

1. 勾选需要分析的模拟记录（最多选择 **3 条**）
2. 选择 1 条记录时，进行**单次分析**，AI 将分析该次模拟的总体情况和可能存在的问题
3. 选择多条记录时，进行**对比分析**，AI 将对比多次模拟的差异和问题
4. 点击 **「提交分析」** 按钮开始分析

**注意：** 只能分析同一布局下的模拟记录。分析过程中需等待，期间无法操作软件。

### 11.3 查看分析结果

分析完成后自动切换到「分析结果」标签页：

- 分析结果以 **Markdown 格式** 渲染显示，包含标题、表格、列表等
- 结果内容可上下滚动查看
- 点击 **「复制」** 按钮可将分析结果复制到剪贴板
- 点击 **「导出MD」** 按钮可将分析结果保存为 Markdown 文件

### 11.4 分析失败与重试

如果分析过程中出现错误（如网络连接失败、AI 服务不可用等）：

- 窗口底部会显示错误提示和简要的失败原因
- 点击 **「重试」** 按钮可重新提交分析

常见失败原因：

| 错误信息 | 可能原因 |
|---------|---------|
| 连接失败 | 网络不通或 AI 服务未启动 |
| AI服务返回错误 (HTTP 401) | API Key 无效或未配置 |
| AI服务返回错误 (HTTP 404) | API 地址配置错误 |
| 请求AI服务失败 (超时) | 网络延迟过高或模型响应过慢 |

### 11.5 历史分析记录

AI 分析结果会自动保存，可在「历史记录」标签页中查看：

1. 点击 **「历史记录」** 标签页
2. 显示所有历史分析记录列表，包含分析时间和类型（单次分析/对比分析）
3. 点击某条历史记录可查看其分析结果
4. 点击记录右侧的 **「✕」** 按钮可删除该历史记录

### 11.6 配置自定义 AI 服务

默认使用内置 AI 服务。如需使用自己的大模型 API，可进行如下配置：

1. 点击菜单栏 **「设置」** → **「设置...」**
2. 在设置对话框中找到 **「AI 分析设置」** 区域
3. 勾选 **「使用自定义API」**
4. 填写以下配置：
   - **API 地址**：兼容 OpenAI 格式的 API 地址，如 `https://api.example.com/v1`
   - **API Key**：API 访问密钥
   - **模型名称**：要使用的模型名称，如 `gpt-4o`、`deepseek-chat` 等
5. 点击 **「测试连接」** 按钮验证配置是否正确
6. 测试成功后，点击 **「保存」** 保存配置

**注意：** 自定义 API 需兼容 OpenAI Chat Completions API 格式。API Key 会加密存储在本地，不会上传到服务器。

---

## 12. AI 自动优化

AI 自动优化功能利用大语言模型对模拟运行结果进行多轮迭代分析，自动调整配置参数并重新运行模拟，逐步优化生产系统性能。

### 12.1 打开 AI 自动优化

1. 在模拟控制面板中，点击 **「AI优化」** 按钮
2. 弹出 AI 优化窗口
3. 如果尚未保存任何模拟记录，需先运行模拟并保存记录

**注意：** AI 优化窗口支持拖动，可拖动标题栏移动窗口位置。

### 12.2 设置优化参数

在 AI 优化窗口中设置以下参数：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 最大迭代次数 | AI 优化循环的最大轮数，每轮包含：分析→调整配置→运行模拟→评估结果 | 5 |
| 选择模拟记录 | 选择一条模拟记录作为优化的初始基准 | 最新记录 |

### 12.3 优化流程

点击 **「开始优化」** 后，系统自动执行以下循环：

1. **AI 分析**：将当前模拟数据发送给 AI，分析瓶颈和优化空间
2. **配置调整**：AI 返回具体的配置变更建议，系统自动应用这些变更
3. **运行模拟**：使用新配置运行模拟，获取新的结果数据
4. **评估结果**：将新结果反馈给 AI，判断是否需要继续优化
5. **循环或停止**：如果 AI 认为还有优化空间且未达到最大迭代次数，继续下一轮；否则停止

### 12.4 AI 可调整的配置参数

AI 在优化过程中可以调整以下参数：

**全局策略参数：**

| 参数 | 中文名称 | 可选值 |
|------|---------|--------|
| resource_selection_rule | 资源选择规则 | basic（基础规则）、min_wip_dynamic（最小在制品动态平衡）、min_utilrate_dynamic（最低利用率动态平衡） |
| product_selection_strategy | 加工制品选择策略 | first_come_first_served（先到先生产）、same_type_priority_with_tool（同类优先兼顾工具）、same_tool_priority（同工具优先） |
| consider_product_priority | 是否考虑产品优先级 | true（是）、false（否） |
| warehouse_selection_priorities | 仓库选择优先级 | nearest_distance、farthest_distance、lowest_utilization、highest_utilization、product_concentrated、product_dispersed、least_waiting_entry |

**设备级参数：**

| 参数 | 中文名称 | 说明 |
|------|---------|------|
| release_mode | 投放模式 | immediate（立即投放）、wait_for_idle（等待空闲） |
| max_capacity | 缓冲区容量 | 正整数，调整缓冲区的最大容量 |
| product_priority | 产品优先级 | 1-5的整数，1为最高优先级 |

**结构性调整：**

| 操作 | 说明 |
|------|------|
| add_buffer（添加缓冲区） | 在瓶颈设备前添加缓冲区，需指定上游设备ID、下游设备ID、容量和产品编码 |
| clone_device（复制瓶颈设备） | 复制瓶颈加工节点以增加产能，需指定设备ID和复制数量（1-3台） |

**注意：** AI 不能修改任何设备的加工时间参数，加工时间是物理约束。

### 12.5 查看优化报告

优化完成后，窗口中显示完整的优化报告，包含：

- **每轮迭代的详细分析**：AI 对当前数据的分析、发现的问题和优化建议
- **配置变更记录**：每轮迭代中 AI 建议并实际应用的配置变更
- **优化效果对比**：各轮迭代的关键指标变化

报告中的利用率数据说明：
- 报告中所有利用率数值已经是百分比形式（范围0-100），例如显示 45.2 表示 45.2%

### 12.6 保存优化布局

1. 优化完成后，点击 **「保存优化布局」** 按钮
2. 在弹出的对话框中选择保存位置和文件名
3. 系统将优化后的画布布局保存为 JSON 文件
4. 保存的布局包含了 AI 优化过程中应用的所有配置变更（包括添加的缓冲区、复制的设备等）

### 12.7 优化过程中的提示信息

优化过程中，窗口底部会显示当前进度信息：

| 提示信息 | 说明 |
|---------|------|
| 第 X/Y 次迭代：AI 分析中... | 正在等待 AI 返回分析结果 |
| 第 X/Y 次迭代：应用配置变更... | 正在应用 AI 建议的配置变更 |
| 第 X/Y 次迭代：运行模拟... | 正在使用新配置运行模拟 |
| ⚠ 添加缓冲区跳过：找不到设备XXX | AI 建议的设备ID在画布中不存在，缓冲区未添加 |
| ⚠ 复制设备跳过：找不到设备XXX | AI 建议的设备ID在画布中不存在，设备未复制 |

### 12.8 JSON 格式验证

AI 返回的配置变更建议必须符合预定义的 JSON 格式。如果 AI 返回的格式不正确：

1. 系统会自动要求 AI 重新生成正确的 JSON
2. 最多重试 10 次
3. 超过 10 次仍无法生成正确格式，则停止优化并提示原因

---

## 13. 文件操作

### 13.1 保存布局

**方法一：菜单保存**
1. 点击「文件」→「保存」
2. 首次保存会弹出保存对话框
3. 选择位置并输入文件名
4. 点击「保存」

**方法二：另存为**
1. 点击「文件」→「另存为」
2. 选择新位置或新文件名
3. 点击「保存」

### 13.2 打开布局

1. 点击「文件」→「打开」
2. 选择 `.json` 格式的布局文件
3. 点击「打开」


### 13.3 文件格式说明

布局文件为 JSON 格式，包含：
- 画布尺寸设置
- 所有设备配置
- 所有连线配置
- 产品定义
- 物料定义

---

## 14. 快捷键参考

| 快捷键 | 功能 |
|--------|------|
| `Delete` | 删除选中对象 |
| `Ctrl + S` | 保存布局 |
| `Ctrl + O` | 打开布局 |
| `Ctrl + N` | 新建画布 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Y` | 重做 |
| `Ctrl + A` | 全选 |
| `Esc` | 取消选择 |

---

## 附录：常见问题

### Q1: 如何创建一个完整的生产线模型？

1. 创建起点，设置产品和生成间隔
2. 创建工作站，设置加工时间和允许产品
3. 用连线连接起点到工作站
4. 继续添加工作站和连线
5. 创建终点，连接到最后的工作站
6. 在产品管理中设置工艺路线

### Q2: 模拟没有产品产出怎么办？

检查以下设置：
1. 起点是否设置了产品
2. 工作站是否允许加工该产品
3. 连线是否正确连接
4. 产品工艺路线是否配置

### Q3: 如何实现产品分流？

1. 从一个设备创建多条连线到不同设备
2. 在模拟设置中选择动态平衡算法
3. 系统会根据算法自动分配产品

### Q4: 仓库容量不足怎么办？

1. 选中仓库
2. 在属性面板增加容量值
3. 或添加临时堆场作为缓冲

---

**技术支持：** [www.enduslab.com](https://www.enduslab.com)

**版权所有 © 2024 上海云图数镜软件技术有限公司**
